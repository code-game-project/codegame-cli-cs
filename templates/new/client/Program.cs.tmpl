namespace Program;

using {{.GameNamePascal}};

class Program
{
	static async Task Main(string[] args)
	{
		var game = await Game.FromArgs(args);

		// TODO: register event listeners

		game.Wait();
	}
}
